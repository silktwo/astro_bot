# Астро-бот «Astra» Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Окремий Telegram-бот, що о 18:00 за Варшавою шле дівчині персональний денний астропрогноз (тропіка + сидерика), відповідає на питання й аналізує надіслані фото/скріни.

**Architecture:** Один Python-сервіс у Docker (`restart: unless-stopped`) на Umbrel. Реальні ефемериди рахує `pyswisseph` локально; LLM лише інтерпретує. Маршрутизація моделей через opencode-go (OpenAI-сумісний): qwen3.7-max — reasoning, claude-sonnet-4.6 — текст, gemini-3.5-flash — vision. Стан у SQLite.

**Tech Stack:** Python 3.11, `python-telegram-bot` (v21+, async), `pyswisseph`, `openai` SDK (проти opencode-go), `APScheduler`, `pytz`, `pytest`, Docker / Portainer.

---

## File Structure

```
astro-bot/
├── src/astro_bot/
│   ├── __init__.py
│   ├── config.py        # читання env, константи моделей/часу
│   ├── ephemeris.py     # pyswisseph: натал + транзити (тропіка/сидерика)
│   ├── llm.py           # клієнт opencode-go: reason()/write()/see()
│   ├── store.py         # SQLite: натал, профіль, історія
│   ├── forecast.py      # конвеєр щоденного прогнозу
│   ├── prompts.py       # шаблони промптів (укр.)
│   └── bot.py           # хендлери Telegram + scheduler + точка входу
├── tests/
│   ├── test_ephemeris.py
│   ├── test_llm.py
│   ├── test_store.py
│   ├── test_forecast.py
│   └── test_bot.py
├── pyproject.toml
├── Dockerfile
├── docker-compose.yml
├── .env.example
└── .gitignore
```

Кожен модуль — одна відповідальність. `bot.py` тримає лише склейку Telegram↔scheduler↔forecast, без бізнес-логіки астрології (вона в `ephemeris`/`forecast`).

---

## Task 0: Каркас проєкту

**Files:**
- Create: `pyproject.toml`, `.gitignore`, `.env.example`, `src/astro_bot/__init__.py`

- [ ] **Step 1: Створити `.gitignore`**

```
__pycache__/
*.pyc
.env
*.db
.venv/
.pytest_cache/
ephe/
```

- [ ] **Step 2: Створити `pyproject.toml`**

```toml
[project]
name = "astro-bot"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "python-telegram-bot>=21.0",
    "pyswisseph>=2.10",
    "openai>=1.40",
    "APScheduler>=3.10",
    "pytz>=2024.1",
    "geopy>=2.4",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-asyncio>=0.23"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["src"]
```

- [ ] **Step 3: Створити `.env.example`**

```
TELEGRAM_BOT_TOKEN=8660523286:REPLACE_ME
OPENCODE_API_KEY=sk-REPLACE_ME
OPENCODE_BASE_URL=https://opencode.ai/zen/go/v1
ADMIN_TELEGRAM_ID=281735938
GF_TELEGRAM_ID=
PUSH_HOUR=18
PUSH_TZ=Europe/Warsaw
SWISSEPH_PATH=/app/ephe
```

- [ ] **Step 4: Створити порожній `src/astro_bot/__init__.py`** (порожній файл).

- [ ] **Step 5: Встановити залежності**

Run: `cd astro-bot && python -m venv .venv && .venv/bin/pip install -e ".[dev]"`
Expected: встановлення без помилок.

- [ ] **Step 6: Commit**

```bash
git add pyproject.toml .gitignore .env.example src/astro_bot/__init__.py
git commit -m "chore: project scaffold"
```

---

## Task 1: `config.py` — конфіг з оточення

**Files:**
- Create: `src/astro_bot/config.py`
- Test: `tests/test_config.py`

- [ ] **Step 1: Failing test**

```python
# tests/test_config.py
import os
from astro_bot import config

def test_loads_models():
    assert config.MODEL_REASON == "qwen3.7-max"
    assert config.MODEL_WRITE == "claude-sonnet-4.6"
    assert config.MODEL_VISION == "gemini-3.5-flash"

def test_push_defaults(monkeypatch):
    monkeypatch.delenv("PUSH_HOUR", raising=False)
    monkeypatch.delenv("PUSH_TZ", raising=False)
    cfg = config.load()
    assert cfg.push_hour == 18
    assert cfg.push_tz == "Europe/Warsaw"
```

- [ ] **Step 2: Run, expect FAIL**

Run: `.venv/bin/pytest tests/test_config.py -v`
Expected: FAIL (module/attrs missing).

- [ ] **Step 3: Implement `config.py`**

```python
import os
from dataclasses import dataclass

MODEL_REASON = "qwen3.7-max"
MODEL_WRITE = "claude-sonnet-4.6"
MODEL_VISION = "gemini-3.5-flash"
MODEL_FALLBACK = "qwen3.7-plus"

@dataclass
class Config:
    telegram_token: str
    api_key: str
    base_url: str
    admin_id: int
    gf_id: int | None
    push_hour: int
    push_tz: str
    swisseph_path: str

def load() -> Config:
    gf = os.getenv("GF_TELEGRAM_ID")
    return Config(
        telegram_token=os.environ.get("TELEGRAM_BOT_TOKEN", ""),
        api_key=os.environ.get("OPENCODE_API_KEY", ""),
        base_url=os.environ.get("OPENCODE_BASE_URL", "https://opencode.ai/zen/go/v1"),
        admin_id=int(os.environ.get("ADMIN_TELEGRAM_ID", "0")),
        gf_id=int(gf) if gf else None,
        push_hour=int(os.environ.get("PUSH_HOUR", "18")),
        push_tz=os.environ.get("PUSH_TZ", "Europe/Warsaw"),
        swisseph_path=os.environ.get("SWISSEPH_PATH", "/app/ephe"),
    )
```

- [ ] **Step 4: Run, expect PASS**

Run: `.venv/bin/pytest tests/test_config.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/astro_bot/config.py tests/test_config.py
git commit -m "feat: config loader"
```

---

## Task 2: `ephemeris.py` — реальні позиції планет (ядро правди)

**Files:**
- Create: `src/astro_bot/ephemeris.py`
- Test: `tests/test_ephemeris.py`

Дані ефемерид мають лежати у `SWISSEPH_PATH`. Для тестів використовуємо вбудований
у pyswisseph Moshier-фолбек (не потребує файлів), тому тести не залежать від `ephe/`.

- [ ] **Step 1: Failing test** (перевіряємо відомі позиції на 2000-01-01 12:00 UT)

```python
# tests/test_ephemeris.py
from astro_bot import ephemeris as e

def test_tropical_sun_j2000():
    # 2000-01-01 12:00 UT: Сонце ~280.4° (Козеріг) у тропіці
    pos = e.planet_positions(2000, 1, 1, 12.0, sidereal=False)
    assert abs(pos["Sun"] - 280.4) < 1.0

def test_sidereal_differs_from_tropical():
    trop = e.planet_positions(2000, 1, 1, 12.0, sidereal=False)["Sun"]
    sid = e.planet_positions(2000, 1, 1, 12.0, sidereal=True)["Sun"]
    # айанамша Lahiri ~23.85° на 2000 → сидеричне довше ~на 24°
    diff = (trop - sid) % 360
    assert 23.0 < diff < 25.0

def test_natal_chart_has_all_planets():
    chart = e.natal_chart(1995, 6, 15, 14.5, 50.45, 30.52, sidereal=False)
    for p in ["Sun", "Moon", "Mercury", "Venus", "Mars",
              "Jupiter", "Saturn", "Uranus", "Neptune", "Pluto"]:
        assert p in chart["planets"]

def test_transits_returns_aspects():
    natal = e.natal_chart(1995, 6, 15, 14.5, 50.45, 30.52, sidereal=False)
    tr = e.transits(natal, 2026, 6, 17, 12.0, sidereal=False)
    assert isinstance(tr["aspects"], list)
```

- [ ] **Step 2: Run, expect FAIL**

Run: `.venv/bin/pytest tests/test_ephemeris.py -v`
Expected: FAIL.

- [ ] **Step 3: Implement `ephemeris.py`**

```python
import os
import swisseph as swe

_PLANETS = {
    "Sun": swe.SUN, "Moon": swe.MOON, "Mercury": swe.MERCURY,
    "Venus": swe.VENUS, "Mars": swe.MARS, "Jupiter": swe.JUPITER,
    "Saturn": swe.SATURN, "Uranus": swe.URANUS, "Neptune": swe.NEPTUNE,
    "Pluto": swe.PLUTO,
}
_ASPECTS = {"conjunction": 0, "sextile": 60, "square": 90,
            "trine": 120, "opposition": 180}
_ORB = 6.0  # градуси

_inited = False
def _init():
    global _inited
    if not _inited:
        path = os.environ.get("SWISSEPH_PATH", "/app/ephe")
        if os.path.isdir(path):
            swe.set_ephe_path(path)
        _inited = True

def _jd(year, month, day, hour_ut):
    _init()
    return swe.julday(year, month, day, hour_ut)

def _flags(sidereal):
    f = swe.FLG_SWIEPH
    if sidereal:
        swe.set_sid_mode(swe.SIDM_LAHIRI)
        f |= swe.FLG_SIDEREAL
    return f

def planet_positions(year, month, day, hour_ut, sidereal=False):
    jd = _jd(year, month, day, hour_ut)
    flags = _flags(sidereal)
    out = {}
    for name, pid in _PLANETS.items():
        lon = swe.calc_ut(jd, pid, flags)[0][0]
        out[name] = lon % 360
    return out

def natal_chart(year, month, day, hour_local, lat, lon, sidereal=False, tz_offset=0.0):
    hour_ut = hour_local - tz_offset
    return {
        "meta": {"year": year, "month": month, "day": day,
                 "hour_local": hour_local, "lat": lat, "lon": lon,
                 "tz_offset": tz_offset, "sidereal": sidereal},
        "planets": planet_positions(year, month, day, hour_ut, sidereal),
    }

def _angle(a, b):
    d = abs(a - b) % 360
    return min(d, 360 - d)

_SIGN_NAMES = ["Aries", "Taurus", "Gemini", "Cancer", "Leo", "Virgo",
               "Libra", "Scorpio", "Sagittarius", "Capricorn", "Aquarius", "Pisces"]

def sign_of(longitude):
    return _SIGN_NAMES[int(longitude % 360 // 30)]

def signs(positions):
    return {name: sign_of(lon) for name, lon in positions.items()}

def transits(natal, year, month, day, hour_ut, sidereal=False):
    today = planet_positions(year, month, day, hour_ut, sidereal)
    aspects = []
    for tname, tlon in today.items():
        for nname, nlon in natal["planets"].items():
            sep = _angle(tlon, nlon)
            for aname, adeg in _ASPECTS.items():
                if abs(sep - adeg) <= _ORB:
                    aspects.append({
                        "transit": tname, "natal": nname,
                        "aspect": aname, "orb": round(abs(sep - adeg), 2),
                    })
    return {"today": today, "aspects": aspects}
```

- [ ] **Step 4: Run, expect PASS**

Run: `.venv/bin/pytest tests/test_ephemeris.py -v`
Expected: PASS (всі 4).

- [ ] **Step 5: Commit**

```bash
git add src/astro_bot/ephemeris.py tests/test_ephemeris.py
git commit -m "feat: ephemeris engine (tropical + sidereal)"
```

---

## Task 2.5: Сід натальної карти Анни + спільні транзити

**Контекст:** карта Анни вже обчислена (`Downloads/Anna_NatalCard.md`) у тропіці й
сидериці. Ми НЕ обчислюємо її з дати народження (її нема в карті) — заносимо
готові натальні довготи напряму. Важливо: **транзитні аспекти інваріантні до системи**
(тропіка/сидерика дають ту саму кутову відстань), тож aspects рахуємо ОДИН раз;
різниця систем — лише у знаках/домах, де сидять планети сьогодні.

**Files:**
- Create: `src/astro_bot/natal_import.py`, `data/anna_natal.json`
- Test: `tests/test_natal_import.py`

- [ ] **Step 1: Failing test**

```python
# tests/test_natal_import.py
import json
from astro_bot.natal_import import sign_deg_to_lon, load_seed

def test_sign_deg_to_lon():
    # Cancer 14°41' = 90 + 14.683 = 104.68
    assert abs(sign_deg_to_lon("Cancer", 14, 41) - 104.683) < 0.01
    # Aquarius 20°09'
    assert abs(sign_deg_to_lon("Aquarius", 20, 9) - 320.15) < 0.01

def test_load_seed_has_planets():
    seed = load_seed("data/anna_natal.json")
    for p in ["Sun", "Moon", "Mars", "Venus", "Saturn", "Pluto"]:
        assert p in seed["planets"]
    assert abs(seed["planets"]["Sun"] - 104.68) < 0.1
```

- [ ] **Step 2: Run, expect FAIL**

Run: `.venv/bin/pytest tests/test_natal_import.py -v`
Expected: FAIL.

- [ ] **Step 3: Implement `natal_import.py`**

```python
import json

_SIGNS = ["Aries", "Taurus", "Gemini", "Cancer", "Leo", "Virgo",
          "Libra", "Scorpio", "Sagittarius", "Capricorn", "Aquarius", "Pisces"]

def sign_deg_to_lon(sign, deg, minute=0):
    idx = _SIGNS.index(sign)
    return idx * 30 + deg + minute / 60.0

def load_seed(path):
    with open(path, encoding="utf-8") as f:
        return json.load(f)
```

- [ ] **Step 4: Створити `data/anna_natal.json`** (тропічні довготи, обчислені з карти)

```json
{
  "name": "Анна",
  "sidereal_signs": {
    "Sun": "Gemini", "Moon": "Leo", "Mercury": "Gemini", "Venus": "Gemini",
    "Mars": "Gemini", "Jupiter": "Taurus", "Saturn": "Taurus",
    "Uranus": "Capricorn", "Neptune": "Capricorn", "Pluto": "Scorpio",
    "Rising": "Virgo"
  },
  "tropical_signs": {
    "Sun": "Cancer", "Moon": "Virgo", "Mercury": "Cancer", "Venus": "Cancer",
    "Mars": "Cancer", "Jupiter": "Gemini", "Saturn": "Taurus",
    "Uranus": "Aquarius", "Neptune": "Aquarius", "Pluto": "Sagittarius",
    "Rising": "Libra"
  },
  "highlights": "Stellium у 9-му домі (Sun, Mercury, Venus, Mars, N Node); Asc Терези/Діва; сильний акцент води; Jupiter-Saturn у 8-му.",
  "planets": {
    "Sun": 104.68, "Moon": 168.68, "Mercury": 104.72, "Venus": 111.57,
    "Mars": 103.28, "Jupiter": 61.27, "Saturn": 57.22, "Uranus": 320.15,
    "Neptune": 305.77, "Pluto": 250.6
  }
}
```

- [ ] **Step 5: Run, expect PASS**

Run: `.venv/bin/pytest tests/test_natal_import.py -v`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src/astro_bot/natal_import.py data/anna_natal.json tests/test_natal_import.py
git commit -m "feat: ingest Anna's precomputed natal chart"
```

---

## Task 3: `store.py` — SQLite-персистенція

**Files:**
- Create: `src/astro_bot/store.py`
- Test: `tests/test_store.py`

- [ ] **Step 1: Failing test**

```python
# tests/test_store.py
from astro_bot.store import Store

def test_save_and_get_natal(tmp_path):
    s = Store(str(tmp_path / "t.db"))
    data = {"year": 1995, "month": 6, "day": 15, "hour": 14.5,
            "lat": 50.45, "lon": 30.52, "tz_offset": 3.0, "city": "Kyiv"}
    s.set_natal(42, data)
    assert s.get_natal(42)["city"] == "Kyiv"

def test_get_missing_natal_is_none(tmp_path):
    s = Store(str(tmp_path / "t.db"))
    assert s.get_natal(99) is None

def test_history_roundtrip(tmp_path):
    s = Store(str(tmp_path / "t.db"))
    s.add_message(42, "user", "привіт")
    s.add_message(42, "assistant", "вітаю")
    hist = s.recent_messages(42, limit=10)
    assert [m["role"] for m in hist] == ["user", "assistant"]
```

- [ ] **Step 2: Run, expect FAIL**

Run: `.venv/bin/pytest tests/test_store.py -v`
Expected: FAIL.

- [ ] **Step 3: Implement `store.py`**

```python
import json
import sqlite3

class Store:
    def __init__(self, path):
        self.conn = sqlite3.connect(path, check_same_thread=False)
        self.conn.row_factory = sqlite3.Row
        self._init()

    def _init(self):
        self.conn.executescript("""
        CREATE TABLE IF NOT EXISTS natal (
            user_id INTEGER PRIMARY KEY,
            data TEXT NOT NULL
        );
        CREATE TABLE IF NOT EXISTS history (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            user_id INTEGER NOT NULL,
            role TEXT NOT NULL,
            content TEXT NOT NULL,
            ts DATETIME DEFAULT CURRENT_TIMESTAMP
        );
        """)
        self.conn.commit()

    def set_natal(self, user_id, data: dict):
        self.conn.execute(
            "INSERT INTO natal(user_id, data) VALUES(?,?) "
            "ON CONFLICT(user_id) DO UPDATE SET data=excluded.data",
            (user_id, json.dumps(data)))
        self.conn.commit()

    def get_natal(self, user_id) -> dict | None:
        row = self.conn.execute(
            "SELECT data FROM natal WHERE user_id=?", (user_id,)).fetchone()
        return json.loads(row["data"]) if row else None

    def add_message(self, user_id, role, content):
        self.conn.execute(
            "INSERT INTO history(user_id, role, content) VALUES(?,?,?)",
            (user_id, role, content))
        self.conn.commit()

    def recent_messages(self, user_id, limit=20):
        rows = self.conn.execute(
            "SELECT role, content FROM history WHERE user_id=? "
            "ORDER BY id DESC LIMIT ?", (user_id, limit)).fetchall()
        return [{"role": r["role"], "content": r["content"]} for r in reversed(rows)]
```

- [ ] **Step 4: Run, expect PASS**

Run: `.venv/bin/pytest tests/test_store.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/astro_bot/store.py tests/test_store.py
git commit -m "feat: sqlite store"
```

---

## Task 4: `llm.py` — клієнт opencode-go з маршрутизацією моделей

**Files:**
- Create: `src/astro_bot/llm.py`
- Test: `tests/test_llm.py`

- [ ] **Step 1: Failing test** (мокаємо OpenAI-клієнт, перевіряємо що роль → правильна модель)

```python
# tests/test_llm.py
from unittest.mock import MagicMock
from astro_bot.llm import LLM
from astro_bot import config

def _fake_client():
    client = MagicMock()
    resp = MagicMock()
    resp.choices = [MagicMock(message=MagicMock(content="ok"))]
    client.chat.completions.create.return_value = resp
    return client

def test_reason_uses_reason_model():
    llm = LLM(client=_fake_client())
    out = llm.reason("аналізуй")
    assert out == "ok"
    kwargs = llm.client.chat.completions.create.call_args.kwargs
    assert kwargs["model"] == config.MODEL_REASON

def test_write_uses_write_model():
    llm = LLM(client=_fake_client())
    llm.write("пиши прогноз")
    assert llm.client.chat.completions.create.call_args.kwargs["model"] == config.MODEL_WRITE

def test_write_falls_back_on_error():
    client = _fake_client()
    good = client.chat.completions.create.return_value
    client.chat.completions.create.side_effect = [Exception("429"), good]
    llm = LLM(client=client)
    assert llm.write("x") == "ok"
    # друга спроба — fallback-модель
    assert client.chat.completions.create.call_args.kwargs["model"] == config.MODEL_FALLBACK

def test_see_sends_image():
    llm = LLM(client=_fake_client())
    llm.see("base64data", "опиши")
    msg = llm.client.chat.completions.create.call_args.kwargs["messages"][0]
    assert any(part["type"] == "image_url" for part in msg["content"])
```

- [ ] **Step 2: Run, expect FAIL**

Run: `.venv/bin/pytest tests/test_llm.py -v`
Expected: FAIL.

- [ ] **Step 3: Implement `llm.py`**

```python
from openai import OpenAI
from astro_bot import config

class LLM:
    def __init__(self, cfg: config.Config | None = None, client=None):
        if client is not None:
            self.client = client
        else:
            self.client = OpenAI(api_key=cfg.api_key, base_url=cfg.base_url)

    def _chat(self, model, messages, fallback=None):
        try:
            r = self.client.chat.completions.create(model=model, messages=messages)
            return r.choices[0].message.content
        except Exception:
            if fallback is None:
                raise
            r = self.client.chat.completions.create(model=fallback, messages=messages)
            return r.choices[0].message.content

    def reason(self, prompt, system=None):
        msgs = ([{"role": "system", "content": system}] if system else []) + \
               [{"role": "user", "content": prompt}]
        return self._chat(config.MODEL_REASON, msgs, fallback=config.MODEL_FALLBACK)

    def write(self, prompt, system=None):
        msgs = ([{"role": "system", "content": system}] if system else []) + \
               [{"role": "user", "content": prompt}]
        return self._chat(config.MODEL_WRITE, msgs, fallback=config.MODEL_FALLBACK)

    def chat(self, messages):
        return self._chat(config.MODEL_WRITE, messages, fallback=config.MODEL_FALLBACK)

    def see(self, image_b64, prompt):
        msgs = [{"role": "user", "content": [
            {"type": "text", "text": prompt},
            {"type": "image_url",
             "image_url": {"url": f"data:image/jpeg;base64,{image_b64}"}},
        ]}]
        return self._chat(config.MODEL_VISION, msgs, fallback=config.MODEL_FALLBACK)
```

- [ ] **Step 4: Run, expect PASS**

Run: `.venv/bin/pytest tests/test_llm.py -v`
Expected: PASS (4 тести).

- [ ] **Step 5: Commit**

```bash
git add src/astro_bot/llm.py tests/test_llm.py
git commit -m "feat: llm client with model routing + fallback"
```

---

## Task 5: `prompts.py` — українські шаблони

**Files:**
- Create: `src/astro_bot/prompts.py`
- Test: `tests/test_prompts.py`

- [ ] **Step 1: Failing test**

```python
# tests/test_prompts.py
from astro_bot import prompts

def test_reason_prompt_includes_aspects_and_both_systems():
    aspects = [{"transit": "Mars", "natal": "Sun", "aspect": "square", "orb": 1.2}]
    signs_trop = {"Sun": "Cancer"}
    signs_sid = {"Sun": "Gemini"}
    p = prompts.reason_prompt(aspects, signs_trop, signs_sid)
    assert "Mars" in p and "square" in p
    assert "Cancer" in p and "Gemini" in p

def test_write_system_is_ukrainian_warm():
    s = prompts.WRITE_SYSTEM
    assert "українськ" in s.lower()
```

- [ ] **Step 2: Run, expect FAIL**

Run: `.venv/bin/pytest tests/test_prompts.py -v`
Expected: FAIL.

- [ ] **Step 3: Implement `prompts.py`**

```python
import json

REASON_SYSTEM = (
    "Ти досвідчений астролог-аналітик. Тобі дають ГОТОВІ, точно обчислені дані: "
    "транзитні аспекти до натальної карти (вони СПІЛЬНІ для обох систем) та "
    "знаки, де сьогодні стоять планети окремо в тропічній і сидеральній системах. "
    "Не вигадуй позицій планет. Виділи 2-3 ключові теми дня з аспектів і поясни, "
    "як по-різному це забарвлюють тропічні vs сидеральні знаки. "
    "Відповідай стисло тезами — це чернетка для подальшого тексту."
)

WRITE_SYSTEM = (
    "Ти теплий, уважний астролог, який пише особистий денний прогноз для однієї "
    "дівчини українською мовою. Тон — turbота, легкість, без езотеричного пафосу "
    "й залякувань. 150-250 слів. Згадай обидві системи (тропічну і сидеральну) "
    "природно, без жаргону. Заверши однією м'якою практичною порадою на день."
)

def reason_prompt(aspects, signs_trop, signs_sid):
    return (
        "Транзитні аспекти до натала (спільні для обох систем):\n"
        + json.dumps(aspects, ensure_ascii=False) +
        "\n\nЗнаки планет СЬОГОДНІ — тропічна система:\n"
        + json.dumps(signs_trop, ensure_ascii=False) +
        "\n\nЗнаки планет СЬОГОДНІ — сидеральна система:\n"
        + json.dumps(signs_sid, ensure_ascii=False) +
        "\n\nВиділи ключові теми дня."
    )

def write_prompt(analysis, name=None):
    who = f" для {name}" if name else ""
    return (
        f"Напиши теплий денний астропрогноз{who} українською на основі цього аналізу:\n\n"
        + analysis
    )
```

- [ ] **Step 4: Run, expect PASS**

Run: `.venv/bin/pytest tests/test_prompts.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/astro_bot/prompts.py tests/test_prompts.py
git commit -m "feat: ukrainian prompt templates"
```

---

## Task 6: `forecast.py` — конвеєр прогнозу

**Files:**
- Create: `src/astro_bot/forecast.py`
- Test: `tests/test_forecast.py`

- [ ] **Step 1: Failing test** (мокаємо LLM, конвеєр кличе reason→write на сід-наталі)

```python
# tests/test_forecast.py
from unittest.mock import MagicMock
from astro_bot.forecast import build_daily

def test_build_daily_pipeline():
    # сід-формат натала: абсолютні тропічні довготи (як у data/anna_natal.json)
    natal = {"name": "Анна", "planets": {
        "Sun": 104.68, "Moon": 168.68, "Mercury": 104.72, "Venus": 111.57,
        "Mars": 103.28, "Jupiter": 61.27, "Saturn": 57.22, "Uranus": 320.15,
        "Neptune": 305.77, "Pluto": 250.6}}
    llm = MagicMock()
    llm.reason.return_value = "аналіз тем дня"
    llm.write.return_value = "Теплий прогноз українською."
    text = build_daily(natal, 2026, 6, 17, llm, name="Анна")
    assert text == "Теплий прогноз українською."
    # reason викликано раніше за write, і write отримав аналіз
    assert "аналіз тем дня" in llm.write.call_args[0][0]
```

- [ ] **Step 2: Run, expect FAIL**

Run: `.venv/bin/pytest tests/test_forecast.py -v`
Expected: FAIL.

- [ ] **Step 3: Implement `forecast.py`**

```python
from astro_bot import ephemeris, prompts

def build_daily(natal_data, year, month, day, llm, name=None, hour_ut=12.0):
    # natal_data["planets"] — абсолютні тропічні довготи (сід-карта)
    natal = {"planets": natal_data["planets"]}
    # Транзитні аспекти СПІЛЬНІ для обох систем (айанамша скорочується),
    # тож рахуємо один раз у тропіці.
    aspects = ephemeris.transits(natal, year, month, day, hour_ut,
                                 sidereal=False)["aspects"]
    # Різниця систем — у знаках, де планети стоять СЬОГОДНІ.
    today_trop = ephemeris.planet_positions(year, month, day, hour_ut, sidereal=False)
    today_sid = ephemeris.planet_positions(year, month, day, hour_ut, sidereal=True)
    signs_trop = ephemeris.signs(today_trop)
    signs_sid = ephemeris.signs(today_sid)
    # reasoning → writing
    analysis = llm.reason(prompts.reason_prompt(aspects, signs_trop, signs_sid),
                          system=prompts.REASON_SYSTEM)
    return llm.write(prompts.write_prompt(analysis, name),
                     system=prompts.WRITE_SYSTEM)
```

- [ ] **Step 4: Run, expect PASS**

Run: `.venv/bin/pytest tests/test_forecast.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/astro_bot/forecast.py tests/test_forecast.py
git commit -m "feat: daily forecast pipeline"
```

---

## Task 7: `bot.py` — онбординг, питання, фото, scheduler

**Files:**
- Create: `src/astro_bot/bot.py`
- Test: `tests/test_bot.py`

Винесемо чисту логіку онбордингу в тестовану функцію `parse_birth(text)`,
а Telegram-склейку лишимо тонкою.

- [ ] **Step 1: Failing test** (парсинг даних народження + геокодинг мокнуто)

```python
# tests/test_bot.py
from astro_bot.bot import parse_birth

def test_parse_birth_full():
    geo = lambda city: (50.45, 30.52, 3.0)  # lat, lon, tz_offset
    d = parse_birth("15.06.1995 14:30 Київ", geocode=geo)
    assert d["year"] == 1995 and d["month"] == 6 and d["day"] == 15
    assert d["hour"] == 14.5
    assert d["city"] == "Київ" and d["lat"] == 50.45

def test_parse_birth_missing_time_defaults_noon():
    geo = lambda city: (50.45, 30.52, 3.0)
    d = parse_birth("15.06.1995 Київ", geocode=geo)
    assert d["hour"] == 12.0
    assert d.get("time_known") is False
```

- [ ] **Step 2: Run, expect FAIL**

Run: `.venv/bin/pytest tests/test_bot.py -v`
Expected: FAIL.

- [ ] **Step 3: Implement `bot.py`** (повний файл)

```python
import base64
import datetime as dt
import re
import logging

from apscheduler.schedulers.asyncio import AsyncIOScheduler
from telegram import Update
from telegram.ext import (Application, CommandHandler, MessageHandler,
                          filters, ContextTypes)
import pytz

from astro_bot import config, prompts
from astro_bot.store import Store
from astro_bot.llm import LLM
from astro_bot.forecast import build_daily

log = logging.getLogger("astro_bot")

_DATE_RE = re.compile(r"(\d{1,2})[.\-/](\d{1,2})[.\-/](\d{4})")
_TIME_RE = re.compile(r"(\d{1,2}):(\d{2})")

def parse_birth(text, geocode):
    """Парсить 'ДД.ММ.РРРР [ГГ:ХХ] Місто'. geocode(city)->(lat,lon,tz_offset)."""
    m = _DATE_RE.search(text)
    if not m:
        raise ValueError("Не бачу дати у форматі ДД.ММ.РРРР")
    day, month, year = int(m[1]), int(m[2]), int(m[3])
    tm = _TIME_RE.search(text)
    if tm:
        hour = int(tm[1]) + int(tm[2]) / 60.0
        time_known = True
    else:
        hour, time_known = 12.0, False
    city = text[m.end():]
    if tm:
        city = city.replace(tm.group(0), "")
    city = city.strip(" ,.-")
    if not city:
        raise ValueError("Не бачу міста народження")
    lat, lon, tz_offset = geocode(city)
    return {"year": year, "month": month, "day": day, "hour": hour,
            "lat": lat, "lon": lon, "tz_offset": tz_offset,
            "city": city, "time_known": time_known}

def _geocode(city):
    from geopy.geocoders import Nominatim
    from timezonefinder import TimezoneFinder
    loc = Nominatim(user_agent="astro-bot").geocode(city)
    if not loc:
        raise ValueError(f"Не знайшов місто: {city}")
    tf = TimezoneFinder()
    tzname = tf.timezone_at(lat=loc.latitude, lng=loc.longitude) or "UTC"
    offset = pytz.timezone(tzname).utcoffset(dt.datetime(2000, 1, 1)).total_seconds() / 3600
    return loc.latitude, loc.longitude, offset

# ── Telegram handlers ───────────────────────────────────────────────
async def cmd_start(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    store: Store = ctx.bot_data["store"]
    uid = update.effective_user.id
    if store.get_natal(uid):
        await update.message.reply_text("Привіт, сонечко 🌙 Я вже знаю твою карту. "
            "Щодня о 18:00 надсилатиму прогноз. Можеш питати будь-що.")
    else:
        await update.message.reply_text("Привіт 🌙 Я твій астро-провідник. "
            "Щоб скласти твою карту — напиши дату, час і місто народження, напр.:\n"
            "`15.06.1995 14:30 Київ`\n(якщо не знаєш точний час — просто дату й місто)")

async def on_text(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    store: Store = ctx.bot_data["store"]
    llm: LLM = ctx.bot_data["llm"]
    uid = update.effective_user.id
    text = update.message.text
    natal = store.get_natal(uid)
    if natal is None:
        try:
            data = parse_birth(text, geocode=_geocode)
        except ValueError as ex:
            await update.message.reply_text(f"{ex}. Спробуй ще раз 🙏")
            return
        store.set_natal(uid, data)
        await update.message.reply_text("Готово ✨ Карту склала. "
            "Сьогодні о 18:00 буде прогноз — або напиши /today прямо зараз.")
        return
    # звичайне питання — у контексті карти + історії
    store.add_message(uid, "user", text)
    history = store.recent_messages(uid, limit=12)
    msgs = [{"role": "system", "content": prompts.WRITE_SYSTEM +
             f"\nКонтекст: натальна карта {natal}"}] + history
    answer = llm.chat(msgs)
    store.add_message(uid, "assistant", answer)
    await update.message.reply_text(answer)

async def on_photo(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    llm: LLM = ctx.bot_data["llm"]
    f = await update.message.photo[-1].get_file()
    buf = await f.download_as_bytearray()
    b64 = base64.b64encode(bytes(buf)).decode()
    caption = update.message.caption or "Що на цьому фото і як це відгукується сьогодні?"
    answer = llm.see(b64, caption)
    await update.message.reply_text(answer)

async def cmd_today(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    store: Store = ctx.bot_data["store"]
    llm: LLM = ctx.bot_data["llm"]
    uid = update.effective_user.id
    natal = store.get_natal(uid)
    if not natal:
        await update.message.reply_text("Спершу надішли дату й місто народження 🙏")
        return
    now = dt.datetime.now()
    text = build_daily(natal, now.year, now.month, now.day, llm)
    await update.message.reply_text(text)

async def cmd_setbirth(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    """Адмін вводить карту дівчини наперед: /setbirth <gf_id> ДД.ММ.РРРР ГГ:ХХ Місто"""
    cfg: config.Config = ctx.bot_data["cfg"]
    store: Store = ctx.bot_data["store"]
    if update.effective_user.id != cfg.admin_id:
        return
    parts = update.message.text.split(maxsplit=1)[1]
    target_id, rest = parts.split(maxsplit=1)
    data = parse_birth(rest, geocode=_geocode)
    store.set_natal(int(target_id), data)
    await update.message.reply_text(f"Карту для {target_id} збережено ✨")

# ── Scheduler ───────────────────────────────────────────────────────
async def _push_daily(app, cfg, store, llm):
    if not cfg.gf_id:
        return
    natal = store.get_natal(cfg.gf_id)
    if not natal:
        return
    now = dt.datetime.now(pytz.timezone(cfg.push_tz))
    text = build_daily(natal, now.year, now.month, now.day, llm)
    await app.bot.send_message(chat_id=cfg.gf_id, text=text)

def build_app(cfg: config.Config, store: Store, llm: LLM) -> Application:
    app = Application.builder().token(cfg.telegram_token).build()
    app.bot_data.update({"cfg": cfg, "store": store, "llm": llm})
    app.add_handler(CommandHandler("start", cmd_start))
    app.add_handler(CommandHandler("today", cmd_today))
    app.add_handler(CommandHandler("setbirth", cmd_setbirth))
    app.add_handler(MessageHandler(filters.PHOTO, on_photo))
    app.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, on_text))
    return app

def main():
    logging.basicConfig(level=logging.INFO)
    cfg = config.load()
    store = Store("/data/natal.db")
    llm = LLM(cfg=cfg)
    app = build_app(cfg, store, llm)

    sched = AsyncIOScheduler(timezone=pytz.timezone(cfg.push_tz))
    sched.add_job(lambda: app.create_task(_push_daily(app, cfg, store, llm)),
                  "cron", hour=cfg.push_hour, minute=0)

    async def _post_init(_):
        sched.start()
    app.post_init = _post_init
    app.run_polling()

if __name__ == "__main__":
    main()
```

- [ ] **Step 4: Додати залежність `timezonefinder`** у `pyproject.toml` (рядок `dependencies`):

```toml
    "timezonefinder>=6.5",
```
Run: `.venv/bin/pip install -e ".[dev]"`

- [ ] **Step 5: Run, expect PASS**

Run: `.venv/bin/pytest tests/test_bot.py -v`
Expected: PASS (2 тести; геокодинг мокнуто через параметр).

- [ ] **Step 6: Прогнати ВСІ тести**

Run: `.venv/bin/pytest -v`
Expected: усі тести PASS.

- [ ] **Step 7: Commit**

```bash
git add src/astro_bot/bot.py tests/test_bot.py pyproject.toml
git commit -m "feat: telegram bot handlers, onboarding, scheduler"
```

---

## Task 8: Docker + деплой

**Files:**
- Create: `Dockerfile`, `docker-compose.yml`

- [ ] **Step 1: Створити `Dockerfile`** (з вбудованими ефемеридами)

```dockerfile
FROM python:3.11-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
    curl ca-certificates && rm -rf /var/lib/apt/lists/*

# Swiss Ephemeris дані (легкий набір, достатній для планет 1800-2400)
RUN mkdir -p /app/ephe && cd /app/ephe && \
    for f in sepl_18.se1 semo_18.se1 seas_18.se1; do \
      curl -fsSL "https://github.com/aloistr/swisseph/raw/master/ephe/$f" -o "$f"; \
    done

WORKDIR /app
COPY pyproject.toml .
COPY src ./src
COPY data ./data
RUN pip install --no-cache-dir .

ENV SWISSEPH_PATH=/app/ephe
CMD ["python", "-m", "astro_bot.bot"]
```

- [ ] **Step 2: Створити `docker-compose.yml`**

```yaml
services:
  astro-bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - astro-data:/data
volumes:
  astro-data:
```

- [ ] **Step 3: Локальний build-тест**

Run: `cd astro-bot && docker build -t astro-bot .`
Expected: образ збирається; ефемериди завантажені у `/app/ephe`.

- [ ] **Step 4: Перевірити ефемериди в образі**

Run: `docker run --rm astro-bot python -c "import swisseph as swe; swe.set_ephe_path('/app/ephe'); print(swe.calc_ut(swe.julday(2026,6,17,12), swe.SUN)[0][0])"`
Expected: число біля 86° (Сонце у Близнюках 17.06.2026), без помилок про відсутні файли.

- [ ] **Step 5: Commit**

```bash
git add Dockerfile docker-compose.yml
git commit -m "feat: dockerize with embedded swiss ephemeris"
```

---

## Task 9: Деплой на Umbrel + жива перевірка

**Files:** —

- [ ] **Step 1:** Скопіювати проєкт на сервер (через scp/git) у `~/astro-bot`.

- [ ] **Step 2:** Створити `~/astro-bot/.env` на сервері з реальними значеннями:
  `TELEGRAM_BOT_TOKEN` (повний токен `8660523286:...`), `OPENCODE_API_KEY`,
  `GF_TELEGRAM_ID` (її Telegram ID), `ADMIN_TELEGRAM_ID=281735938`,
  `PUSH_HOUR=18`, `PUSH_TZ=Europe/Warsaw`. Файл — права 600, у git не потрапляє.

- [ ] **Step 3:** Підняти через Portainer (Stacks → from compose) або
  `docker compose up -d --build`. Перевірити `restart: unless-stopped`.

- [ ] **Step 3.5: Засідити карту Анни** — у контейнері виконати разовий скрипт,
  що бере `data/anna_natal.json` і кладе її в `Store` під `GF_TELEGRAM_ID`:

```bash
docker compose exec astro-bot python -c "
import os, json
from astro_bot.store import Store
from astro_bot.natal_import import load_seed
s = Store('/data/natal.db')
s.set_natal(int(os.environ['GF_TELEGRAM_ID']), load_seed('/app/data/anna_natal.json'))
print('seeded')
"
```
(потребує, щоб `data/` копіювалася в образ — додати `COPY data ./data` у Dockerfile.)

- [ ] **Step 4: Жива перевірка** — від акаунту Анни написати боту `/today`;
  очікувати прогноз українською: спільні транзитні аспекти + подвійне трактування
  знаків (тропіка ♋ / сидерика ♊ тощо). Онбординг для неї не потрібен — карта вже є.

- [ ] **Step 5: Перевірка vision** — кинути боту фото з підписом; очікувати осмислену відповідь.

- [ ] **Step 6: Перевірка пушу** — тимчасово виставити `PUSH_HOUR` на найближчу
  хвилину (або викликати `_push_daily` вручну), переконатися що о 18:00 за Варшавою
  приходить прогноз її акаунту; повернути `PUSH_HOUR=18`.

- [ ] **Step 7: Перевірка автостарту** — `docker restart` контейнера і ребут хоста;
  переконатися що бот сам піднявся (политика `unless-stopped`).

- [ ] **Step 8:** Перевипустити `OPENCODE_API_KEY` через провайдера (старий засвічено в чаті),
  оновити `.env`, перезапустити контейнер.

---

## Self-Review

**Spec coverage:**
- Окремий бот (8660523286) → Task 7/9. ✅
- Реальні ефемериди тропіка+сидерика → Task 2, вбудовані дані → Task 8. ✅
- Модель-split qwen3.7-max → sonnet-4.6 → Task 4 (routing) + Task 6 (pipeline). ✅
- Vision gemini-3.5-flash → Task 4 (`see`) + Task 7 (`on_photo`). ✅
- Українська мова → Task 5. ✅
- Онбординг + адмін-ввід наперед → Task 7 (`parse_birth`, `cmd_start`, `cmd_setbirth`). ✅
- Памʼять/контекст → Task 3 + Task 7 (`on_text` history). ✅
- Пуш 12:00 Europe/Warsaw → Task 7 (scheduler) + Task 9 step 6. ✅
- Окремий API-ключ через env, не в git → Task 0 (.env.example) + Task 9. ✅
- Автостарт при рестарті → Task 8 (`restart: unless-stopped`) + Task 9 step 7. ✅
- opencode-go endpoint → Task 1/4. ✅

**Placeholder scan:** немає TBD/«handle errors» без коду; fallback реалізовано в Task 4.

**Type consistency:** `build_daily(natal, y, m, d, llm, name)`, `LLM.reason/write/see/chat`,
`Store.get_natal/set_natal/add_message/recent_messages`, `parse_birth(text, geocode)` —
імена збігаються між визначенням і викликами.

## Поза обсягом (YAGNI)
Веб-дашборд, мультиюзер, графічні карти, TTS — не реалізуємо.
